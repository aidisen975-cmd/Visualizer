# Visualizer

当前版本：**0.2.4**  
工程格式：`projectFormatVersion: "1.0"`  
正式版：[v0.2.4](https://github.com/aidisen975-cmd/Visualizer/releases/tag/v0.2.4)

浏览器本地运行的实验数据可视化工作台，面向科研与工程出图。数据在本机处理，不经过远程服务器，也不需要安装客户端或注册云端账户。Windows 与 macOS 使用同一份文件。

本文只写**当前版本已经能用的能力**。功能增加或收缩时同步改这一份文件，不把规划写成已上线。完整开发过程见 [DEVLOG.md](DEVLOG.md)，当前开发状态见 [Progress/PROGRESS-v0.2.4.md](Progress/PROGRESS-v0.2.4.md)。

## 特色

- **打开即用**：解压后用 Chrome 或 Edge 打开 HTML，无需安装、无需联网即可导入、绘图和导出。
- **工程可带走**：保存为自包含 `.tvproj.json`，原始数据与排图状态都写进文件。原 CSV 移动或删除后仍可打开；同一工程可在 Windows 与 macOS 之间继续编辑。
- **文件不是图**：Dataset、Series、Plot、Canvas 分开。一张图可以叠多个数据源的曲线；跨文件同名列不会互相覆盖。每条曲线保留独立横纵坐标，不强制共用同一时间轴。
- **画布即导出区**：预览、PNG、SVG 使用当前画布内容和布局，不另做一套导出版式。
- **通用数值 CSV**：不限于温度表。能解析 `Name(unit)`、`Name [unit]`、`Name / unit` 表头，不猜测未写出的单位。
- **出图可控**：多图排版、磁吸对齐、图例位置、曲线与文字样式、坐标轴刻度都可以直接改，适合整理论文和汇报图。

## 功能（v0.2.4）

### 数据导入与管理

- 导入通用数值 CSV；纯文本列会跳过并提示，不导致整表失败。
- 数据按数据源分成数据簇，可折叠 / 展开。簇标题显示已选数量，勾选不会因折叠丢失。每个簇可以一次性把颜色、线型或线宽写入该簇全部 Series；之后仍可单独改某一条，除非再次批量应用。
- 数据列与图中 Series 都支持全选、全不选、Shift 连选、Ctrl/Cmd 点选；两边规则相同，选择集互相独立。
- 曲线区分原始列名与显示名。右侧 Series 行分层显示两者，超长名称省略且不撑破面板；支持单条重命名，以及查找替换、前后缀、`T{n}` / `{original}` 批量重命名（应用前可预览）。
- 右侧可单独勾选要编辑的 Series，与图上的显示 / 隐藏分开：改样式或重命名不会误改可见性。

### 绘图与排版

- 打开或新建工程时自动给出 Figure 1。
- 同一画布可放多张图；图可创建、选择、移动、缩放、删除。
- 布局模板：单图、1:1、左大右小、左小右大、三等分、一大两小、2×2、3×2。一大两小可指定当前主图，重新排列时不销毁已有图。
- 磁吸对齐：画布边、其他图的边缘与中心、缩放边，以及等间距候选。
- 左、中、右栏可拖动改宽、双击恢复默认宽度、折叠；栏宽写入工程。
- 画布缩放 25%–300%，与图表真实尺寸分离。提供适应窗口、100%，以及 Ctrl/Cmd + 滚轮、`+` / `-` / `0`。
- 属性栏宽度 / 高度绑定图表真实尺寸；拖角缩放与数值输入同步，可锁定宽高比。新建图默认 1280×800。
- 图标题、轴标题、刻度按实际文字尺寸自动留白；字号变大时 Plot Area 内缩，必要时才增大 Figure，避免标题溢出。图标题可左对齐、居中或右对齐。

### 图例、样式与坐标轴

- 图例可开关，支持停靠位置、自由拖动、字体、行 / 列、线样与最大宽度。列数显式指定时按列换行。自由位置可单独设置背景颜色、透明度与边框，不影响文字和线样透明度。
- 曲线样式：颜色、线型、线宽 0.5–12 px（0.1 步进）、透明度；可批量改线型 / 线宽 / 透明度，多选线宽不同时显示混合态。颜色只在明确设置时覆盖。颜色可以输入或粘贴 HEX。线型菜单用与绘图相同的虚线样式做预览。
- 文字样式：图标题、轴标题、刻度、图例；字体含系统默认、微软雅黑、宋体、Arial、Times New Roman。轴线颜色与宽度可改。
- 轴标题支持自动识别与手动编辑；用户改过的标题不会被后续加曲线覆盖。
- 坐标范围为自动或手动。X 轴自动范围贴齐数据，左侧从 0 开始不加空白；切到手动时预填当前范围。
- 每张图可独立设置时间显示单位（含 ms）；换算只发生在显示层，不改写原始数据。
- 刻度可自动或手动：主间隔、次刻度、小数位。非法间隔或主刻度超过 500 会被拒绝。

### 工程、预览与导出

- 新建、打开、保存、另存为工程。
- Chrome / Edge 可使用系统文件对话框；Safari / Firefox 保存时下载到浏览器默认目录。
- PNG、SVG、另存为走同一套保存逻辑。
- 可打开旧版工程；缺失字段在加载时补默认值，不要求手动升级文件。
- 标题栏「关于」显示当前软件版本和工程格式。两者分开，软件更新不要求升级工程。
- 「检查更新」只在点击后访问 GitHub Releases。可以查看更新说明，并打开对应的 `Visualizer-vX.Y.Z.zip`。没有正式包时改为打开 Release 页面。下载后不会自动覆盖当前文件，需要自行解压并用新版本打开。
- 断网时导入、绘图、保存、打开工程、预览和导出不受影响。检查更新失败只留在关于面板里说明。

## 如何使用

1. 下载正式版 [Visualizer-v0.2.4.zip](https://github.com/aidisen975-cmd/Visualizer/releases/tag/v0.2.4) 并解压，或直接使用本仓库根目录文件。
2. 保持以下两个文件在**同一文件夹**：
   - `temperature_trajectory_visualizer.html`
   - `visualizer-core.js`
3. 用 Chrome 或 Edge 打开 HTML（Windows / macOS 步骤相同）。双击即可，一般不需要本地服务器。
4. 导入 CSV，或打开已有 `.tvproj.json`。
5. 排图后可预览，并导出 PNG / SVG；继续编辑请保存工程。

开发调试也可在仓库根目录执行 `python3 -m http.server` 后访问该页面。核心数据测试：`node tests/core-test.js`。

## 当前限制

- PNG / SVG 导出整张画布，不能只导出选中的一张图。
- 画布缩放只影响编辑视口，不写入导出图像。
- 布局模板作用于当前画布上的全部图。
- 等间距吸附会给出对齐，辅助线上暂无间距数值标签。
- 尚无撤销 / 重做。
- 图例自由拖动时，请用右侧眼睛按钮切换显隐。
- 窗口过窄时，三栏可能挤压画布。
- 无系统文件对话框的浏览器只能保存到默认下载目录。
- 本机没有所选中文字体时使用 fallback，不会报错。
- 检查更新只打开 GitHub Release 或对应压缩包，不会替换当前已经打开的文件。

## 本版本尚未提供

以下能力仍在后续规划中，当前界面不作为已完成功能使用：公式列、MAE / RMSE、Results、图拆分、局部放大、Inset、标注编辑、Cursor / Data Inspector、Undo、双 Y 轴、轴联动、布局锁定、Heatmap、3D。本工具也不做 Origin 级统计分析、拟合、云同步或桌面安装包。

## 文档约定

`README.md` 跟随软件版本改，不另存历史副本：

- 新功能写入对应小节；
- 删除、收缩或尚未交付的功能从本文拿掉或移入「尚未提供」；
- 文首版本号与 `Progress/PROGRESS-v{softwareVersion}.md` 保持一致。
